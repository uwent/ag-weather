AgWeather status (<%= Rails.env %>)
=============================================================
Messages for <%= (Date.today - 1).strftime("%m/%d/%Y") %>:
<% @statuses.each do |status| %>
  <%= status %>
<% end %>
