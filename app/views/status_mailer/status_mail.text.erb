AgWeather problem on <%= ENV["SERVER_ROLE"] %> (<%= ENV["AG_WEATHER_HOST"] %>)
=============================================================
Messages for <%= Time.current %>:
<% @statuses.each do |status| %>
  <%= status %>
<% end %>
