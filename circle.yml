machine:
  ruby:
    version: 2.2.2

dependencies:
  pre:
    - sudo apt-get update
  post:
    - npm install -g dredd
    - sudo apt-get install libgrib-api-tools

test:
  post:
    - RAILS_ENV=test bundle exec rake dredd
